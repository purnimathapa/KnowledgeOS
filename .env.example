NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Google AI Studio key (starts with AIza…): https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional if 2.0-flash hits free-tier limits: gemini-2.0-flash-lite or gemini-1.5-flash
# GEMINI_MODEL=gemini-2.0-flash-lite

# Optional: skip confirmation emails during local dev (Dashboard → Settings → API → service_role).
# Never expose this key to the browser or commit .env.local.
SUPABASE_SERVICE_ROLE_KEY=
